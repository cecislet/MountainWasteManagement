function on_message(self, message_id, message, sender)
	if message_id == hash("update_coins") then
		local label = gui.get_node("score_label")
		gui.set_text(label, "Coins: " .. message.coins)
	end
end